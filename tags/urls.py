from rest_framework import routers
from .views import TagViewSet, TagProfileViewSet


router = routers.SimpleRouter()
# router.register(r'tags', TagViewSet)
# router.register(r'tagsProfile', TagProfileViewSet)
